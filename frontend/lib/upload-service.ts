export async function handleUpload(file: File): Promise<void> {
  // TODO: POST the file to the backend upload endpoint once it is available.
  void file;
  await new Promise((resolve) => setTimeout(resolve, 0));
}

export async function analyzeDocuments(documentNames: string[]): Promise<void> {
  // TODO: Send the uploaded document references to the AI analysis pipeline
  // when the backend is implemented.
  void documentNames;
  await new Promise((resolve) => setTimeout(resolve, 0));
}
